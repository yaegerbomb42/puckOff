# SwarmConnect (Port 8081)
server {
    listen 8081;
    server_name _;
    root /usr/share/nginx/html/swarmconnect;
    index index.html;
    try_files $uri $uri/ /index.html;
}

# Yaeger.info (Port 8082)
server {
    listen 8082;
    server_name _;
    root /usr/share/nginx/html/yaeger-info;
    index index.html;
    try_files $uri $uri/ /index.html;
}

# Raidball (Port 8083 - .com, .info, .online, .store)
server {
    listen 8083;
    server_name _;
    root /usr/share/nginx/html/raidball;
    index index.html;
    try_files $uri $uri/ /index.html;
}

# MyNow.online (Port 8084)
server {
    listen 8084;
    server_name _;
    root /usr/share/nginx/html/mynow;
    index index.html;
    try_files $uri $uri/ /index.html;
}

# SwarmAgents.codes (Port 8085)
server {
    listen 8085;
    server_name _;
    root /usr/share/nginx/html/swarmagents;
    index index.html;
    try_files $uri $uri/ /index.html;
}

# TurboToolbox.me (Port 8086)
server {
    listen 8086;
    server_name _;
    root /usr/share/nginx/html/turbotoolbox;
    index index.html;
    try_files $uri $uri/ /index.html;
}
